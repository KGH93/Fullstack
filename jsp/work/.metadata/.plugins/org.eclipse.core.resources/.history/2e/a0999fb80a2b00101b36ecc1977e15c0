/* 확대 효과 (2배 확대) */
  .hover-zoom {
    transition: transform 0.3s ease;
  }

  .hover-zoom:hover {
    transform: scale(2);
    z-index: 10;
    position: relative;
  }

  /* 이미지 간격 최소화 */
  .film-strip img {
    width: 150px;
    height: 100px;
    object-fit: cover;
    margin-right: 4px;
  }

  .film-strip {
    gap: 4px;
  }