#include <stdio.h>
#include <stdbool.h>  //c++에서 주로사용 불린값 쓸때

int main(){
    bool isProgrammingFun = true;
    bool isFishTasty = true;

    printf("%d",isProgrammingFun == isFishTasty);
    
    return 0;
}